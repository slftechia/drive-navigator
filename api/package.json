{
  "name": "drive-nav-api",
  "version": "0.2.0",
  "private": true,
  "description": "Drive Navigator API — Express (Render)",
  "main": "dist/src/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "start": "node dist/src/index.js",
    "prestart": "npm run build",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.4",
    "rimraf": "^6.1.3",
    "typescript": "^5.7.3"
  }
}
